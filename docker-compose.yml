services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: smart_failure
      POSTGRES_USER: smart_failure
      POSTGRES_PASSWORD: smart_failure
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
